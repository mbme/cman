#!/usr/bin/env ruby

require 'cman'

command = ARGV[0]
unless Cman.respond_to?(command)
  log.error "wrong command '#{command}'"

  methods = Cman.methods(false).map { |s| s.to_s }.join ', '
  log.error "must be one of #{methods}"

  return 1
end

# TODO: handle command
puts 'done'
Cman.status
