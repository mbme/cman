#!/usr/bin/env ruby

require 'cman/executor'
require 'cman/logger'

include Cman::Logger

begin
  Cman.load_config

  command = Cman::Executor.new ARGV[0]
  command.execute(*ARGV[1..-1])

  exit 0
rescue => e
  error e.message
  exit 1
end
